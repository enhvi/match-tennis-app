export async function scheduleConfirmedMatchReminder(requestId, request, prefs) {
  // Notifications are currently disabled app-wide.
  return;
}

export async function cancelMatchReminder(requestId) {
  // Notifications are currently disabled app-wide.
  return;
}
